# 项目协作规范

## 一、项目主线

本项目的核心目标是：

> 构建一个面向 Windows 本机运行的账号工作流控制台，使用 Vue 3、FastAPI 和 MongoDB，统一管理邮箱、代理、浏览器任务、账号状态、支付工具和成品。

所有开发、重构和功能请求都必须服务于这个目标。与主线无关的改动先记录，不自行扩大范围。

当前阶段目标、验收标准和暂不处理事项统一记录在 `docs/task-state.md`。

## 二、开发规范

1. 修改代码前先阅读相关模块、现有实现和测试。
2. 优先复用现有架构、工具、命名方式和错误处理模式。
3. 每次修改只解决一个明确问题，避免顺手重构无关代码。
4. 新增功能必须考虑错误处理、边界条件、兼容性和回滚方式。
5. 不删除或覆盖用户已有改动。
6. 修改前保留必要的备份点，或确保 Git 可以回滚。
7. 不提交密钥、密码、Token、邮箱凭据、代理凭据、Access Token、TOTP Secret 或第三方 API Key。
8. 提交前检查格式、类型、测试、敏感信息和 Git diff。

## 三、Git 与 PR 规范

1. 每个独立任务完成后创建清晰、可回滚的 Git commit。
2. commit 信息说明实际变更，避免使用 `update`、`fix` 这类无上下文描述。
3. 需要合并到主分支的任务必须提交 Pull Request；已有 PR 则继续更新对应 PR，不重复创建。
4. PR 描述必须包含变更摘要、验证方式、影响范围和已知风险。
5. 提交 PR 前确认工作区 diff、测试结果和敏感信息检查均已完成。
6. 没有完成 commit 和 PR 的任务，只能标记为“代码完成”，不能标记为“交付完成”。

## 四、任务执行规范

每个任务开始前，先明确：

- 任务目标
- 涉及文件
- 不涉及的范围
- 验收标准
- 验证命令

执行过程中：

- 遇到不确定内容，优先从代码、测试、文档和 Git 历史中确认。
- 不因为发现旁支问题就改变当前任务。
- 发现需要扩大范围的修改时，先记录，不自动展开。
- 每完成一个阶段，更新 `docs/task-state.md`。

任务结束时必须说明：

- 做了什么
- 改了哪些文件
- 如何验证
- 是否还有遗留问题或风险

## 五、AI 工作规范

AI 每次开始工作前必须读取：

1. 本文件
2. 当前任务说明
3. 相关代码和测试
4. 最近的 Git diff
5. `docs/task-state.md`

AI 必须始终保留以下上下文：

- 项目主线
- 当前任务
- 已完成内容
- 未完成内容
- 当前阻塞点
- 唯一下一步动作

如果上下文可能丢失，先更新 `docs/task-state.md`，再继续工作。

AI 不得：

- 擅自改变任务目标
- 把“分析”变成“重构”
- 把单点修复扩展成大规模架构改造
- 虚构测试结果、运行结果或文件内容
- 在没有验证的情况下声称任务完成

## 六、任务状态文件

`docs/task-state.md` 是项目的外部工作记忆，至少记录：

- 当前目标
- 当前阶段
- 已完成
- 进行中
- 待处理
- 决策记录
- 验收标准
- 下一步唯一动作

每次重要修改后更新一次。

## 七、完成定义

只有满足以下条件，任务才算完成：

- 需求目标已实现
- 相关测试或验证已执行
- 没有引入明显回归
- Git diff 已检查
- 文档或配置已同步
- 已明确说明剩余风险
- 已创建或更新对应 Pull Request（仅限需要合并的任务）
